[package]
name = "cif-stage0"
version = "0.1.0"
edition = "2021"
description = "CIF Brain Stage 0 — bootstrap experiment"

[dependencies]
strand-core = { path = "../strand-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
